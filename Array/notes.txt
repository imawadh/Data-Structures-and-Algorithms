1-D Array :::
    Phase 1 :
        What is Array ?
        Why did we need Array ?
        Syntax and Declaration::
            int x[numberOfElement];
        syntaxAndDeceleration.cpp
        arrayInitialization.png
        Accessing  an Element of Array of Vector 
        How to take input for elements of array ?
        Syntax Basic ::
            int a = {1,2,3}; ---> Incorrect     
            int a[] = {1,2,3}; ---> Correct     
            int a[4] = {1,2,3}; ---> Correct {Partial Initialization}     
            int a[2] = {1,2,3}; ---> InCorrect 
            int size = 10, arr[size]; ---> Correct
        largestElementArray.cpp
        secondLargestElementArray.cpp
        thirdLargestElementArray.cpp

    Phase 2 :
        Array is pass by reference when passed to function
        Another reference is passed to same array when passing to a function and no need to pass &arrayName to pass it by pass reference.
            passByReferenceArray.cpp ->
            we can also recieve int *arrayName as paramater in the function.

        When pass by reference we use . operator... 
        
        int *ptr = arr;  -> sahi hai // passing address
        int *ptr = &arr; -> galat hai // passing address of address 
        int *ptr = arr[0]; -> galat hai // passing value instead of address
        int *ptr = &arr[0]; -> sahi hai // passing address
        1. 
            for(int i = 0; i<5; i++){
                cout<<arr[i]<<' ';
            }        
        
        2.
            int* ptr = arr;
            for(int i =0; i<5; i++){
                cout<<*ptr<<' ';
                ptr++;
            }
        
        3.
            for(int i =0; i<5; i++){
                cout<<i[ptr]<<' ';
            }
        
        4.
            int* ptr = arr;
            for(int i =0; i<5; i++){
                cout<<*ptr[i]<<' ';
            }

        1, 2, 3, 4 will will result in same output.... 


        Vector --> It is of dynamic size while array was not.

        vector.size()  V/s v.capcity 
        new capacity = oldSize * 2; 

        1. vector <int> v(5) means size of vector and capacity of vector is 5 only...
        2. v.at(index)
        3. vectors are pass by value by default, we use & to make it pass by reference.
        
        Rotate an array by k steps ::
            step 1 : k = k%n;
            step 2 : reverse(0,n-k-1); // start ke n-k elements ko reverse
            step 3 : reverse(n-k,n-1); // antim ke k elements ko reverse
            step 4 : reverse(0,n-1); // complete array reverse 

    Phase 3 :
        1. Sort Arrays of Zeroes and Ones.
            sortZeroesANsOnes.cpp
        2. Sort Color
        3. Merge Two Sorted array 
            Leetcode 88
            Try Doing in same array
        4. Next Permutation
            LeetCode 31
            step 1 : Find pivot element 
            step 2 : Find the first greater element than pivot element from right to pivot 
            step 3 : swap and break after we got the first grater elemnt from right 
            step 4 : reverse the pivot+1 to n-1 array 
            nextPermuataion.cpp

        5. Trapping Rainwater
            LeetCode 42
        
2-D Array 
    Phase 1 :
                


