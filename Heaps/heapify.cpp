// NOT DONE 
// Heapify means to convert given n number of elements and convert it min heap or max heap 

#include<bits/stdc++.h>
using namespace std;
void heapify(vector<int>&v){
    int n = v.size();
    for(int i =n-1; i>=0; i--){

        int left = 2*i+1; 
        int right = 2*i+2; 
        if(left<n && right<n){

        }
        else if(left<n){

        }
        else {
            // Do nothing if left doesn't exist right will never exist 
        }
    }
}
int main(){

}